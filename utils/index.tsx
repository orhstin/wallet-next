import { Cluster, Keypair } from "@solana/web3.js";
import { message } from "antd";

// Implement a function to get account balance.

const refreshBalance = async (network: Cluster | undefined, account: Keypair | null) => { return 0;};
const handleAirdop = async (network: Cluster, account: Keypair | null) => {};

export { refreshBalance, handleAirdop };